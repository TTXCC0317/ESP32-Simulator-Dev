# ESP32 网页版终端模拟器 — 实施方案

> 版本：v0.2（待评审 — 按用户反馈重写：双引擎并行、测试验收随里程碑走）
> 日期：2026-08-28
> 前置文档：《01-总体设计方案》（已批准）
> 本文档取代 v0.1 的集中式测试/验收文档，测试与验收标准内嵌于各里程碑。

---

## 1. 开发环境与前置依赖

### 1.1 本机环境要求

| 组件 | 版本 | 用途 |
|---|---|---|
| Node.js | 22 LTS（>=22.13） | 前后端运行时；pnpm 11 最低要求 |
| pnpm | 11+（corepack 自动固定） | Monorepo 包管理 |
| Git | 2.x | 版本管理 |
| 浏览器 | Chrome / Edge 最新两个大版本 | 开发与验收 |
| Python | 3.11+（可选） | esptool 运行环境 |

### 1.2 外部工具链（引擎B）

| 工具 | 获取方式 | 所属里程碑 |
|---|---|---|
| arduino-cli | 官方安装脚本 | M4 |
| esp32 板卡支持包 | `arduino-cli core install esp32:esp32` | M4 |
| esptool | `pip install esptool` | M4 |
| QEMU（Espressif fork esp-develop 9.x 预编译包） | GitHub Releases | M4（UART）/ M5（GPIO 桥） |
| lcgamboa/qemu fork（picsimlab-esp32 分支，含 GPIO 导出） | 源码构建或 Velxio 预编译 | M5 |

> 工具链路径统一在 `config/app.json` 声明；后端启动自检并通过 `GET /api/health/tools` 暴露。缺失时引擎B入口置灰并给安装指引，引擎A不受影响。

#### 1.2.1 lcgamboa/qemu fork 在 Windows 上的构建（MSYS2）

> 仅当 M5 决定走 lcgamboa fork 自行构建时执行；优先评估 Velxio 预编译包或 Espressif fork 是否已满足 GPIO 导出需求，能不复用源码构建就跳过本节。

依赖：MSYS2（pacman）+ mingw-w64 工具链。整个流程在 `D:\qemu-build` 根目录（路径不含空格）下、mingw64 shell 中执行：

```bash
# 1. 安装 MSYS2 后，在 mingw64 shell 中安装依赖
pacman -S --needed mingw-w64-x86_64-{gcc,make,ninja,pkg-config,python} git

# 2. 克隆 lcgamboa fork 并切到 picsimlab-esp32 分支
git clone https://github.com/lcgamboa/qemu.git
cd qemu && git checkout picsimlab-esp32

# 3. 配置（仅构建 xtensa softmmu，目标机器 esp32）
./configure --target-list=xtensa-softmmu --enable-slirp --disable-tools --disable-docs

# 4. 编译（Ninja 并行）
ninja -C build

# 5. 产物 build/qemu-system-xtensa.exe，移动到 config/app.json 的 tools.qemuXtensa 路径
```

常见问题：`slirp` 缺失时去掉 `--enable-slirp`（仅影响 WiFi 网络后端，一期 WiFi 走语义模拟可忽略）；configure 阶段若报 `python not found`，确认 `mingw-w64-x86_64-python` 已装；产物运行时缺 DLL，把 `msys2/mingw64/bin` 加入 PATH 或拷贝所需 DLL 到产物目录。

### 1.3 工具链自检响应

```json
{
  "node": "20.11.0",
  "arduinoCli": { "ok": true, "version": "1.1.1", "esp32Core": "3.1.1" },
  "esptool": { "ok": true, "version": "4.8" },
  "qemu": { "ok": false, "reason": "config.app.json 中 tools.qemuXtensa 未配置" }
}
```

### 1.4 可观测性与监控

| 维度 | 实现 | 暴露方式 |
|---|---|---|
| 后端健康 | `/api/health` 返回进程 uptime/db 连接/工具链状态 | REST |
| 工具链状态 | `/api/health/tools`（§1.3） | REST |
| QEMU 异常发现 | QemuManager 监听子进程 `exit` 事件，非零退出立即转 `state=error` 并把退出码+stderr 尾 2KB 推 WS `log.error` | WS + REST `GET /api/sessions/:id` |
| 编译任务指标 | builds 表记录 status/started_at/finished_at；`GET /api/builds?status=running` 仪表板 | REST |
| WS 会话指标 | 在线会话数、各会话状态、最近事件计数；`GET /api/metrics/sessions` | REST（不暴露 Prometheus，单机定位） |
| 结构化日志 | pino，JSON 行式输出到 `logs/server.log` + stdout，按级别/info+warn+error 分文件轮转 10MB×5 | 文件 |
| 前端错误 | window.onerror/promise rejection → POST `/api/ingest/frontend-error`（限频 1 条/10s） | REST |

> 不引入 Prometheus/Grafana：单机定位，REST 仪表板 + 文件日志足够；P3 评估是否升级。

---

## 2. 仓库结构（pnpm Monorepo）

```
ESP32Simulator/
├── documents/                    # 设计文档集（01~06）
├── apps/
│   ├── web/                      # 前端（React 18 + Vite + TS）
│   │   └── src/
│   │       ├── routes/           # 工作台 / 工程列表
│   │       ├── circuit/          # canvas / library / inspector / wiring
│   │       ├── editor/           # Monaco 封装与文件 Tab
│   │       ├── serial/           # 串口监视器（xterm.js）
│   │       ├── analyzer/         # 逻辑分析仪（M11）
│   │       ├── sim/              # PinBus / engine 接口 / mpy-wasm 引擎 / qemu-bridge 引擎
│   │       ├── stores/           # Zustand：project / circuit / sim / ui
│   │       ├── theme/            # 双主题 token + ThemeProvider
│   │       ├── api/              # REST 客户端
│   │       └── assets/parts/     # 元件 SVG 资源（与《05-元件清单》一一对应）
│   └── server/                   # 后端（Fastify + TS）
│       └── src/
│           ├── routes/           # projects / parts / boards / build / examples / health
│           ├── ws/               # 仿真会话网关（引擎B）
│           ├── services/         # project / build / qemu.manager / catalog
│           ├── db/               # better-sqlite3 + migrations/
│           └── config.ts
├── packages/shared/              # 前后端唯一协议与类型源
│   └── src/                      # circuit.ts / ws-protocol.ts / engine.ts / catalog.ts
├── config/
│   ├── app.json                  # 端口/路径/超时/并发
│   ├── boards/*.json             # 板卡定义（引脚表见《05-元件清单》）
│   └── parts/*.json              # 元件定义（见《05-元件清单》）
├── examples/                     # 内置示例（每个含 golden.json 测试剧本）
├── tools/                        # setup-toolchain.ps1 / seed-test-db.ps1
├── package.json / pnpm-workspace.yaml / tsconfig.base.json
```

### 2.1 `pnpm-workspace.yaml` 内容

```yaml
packages:
  - 'apps/*'
  - 'packages/*'
```

> 仅声明两个 workspace 目录；config/ tools/ examples/ documents/ 不入 workspace（前者是运行期配置/数据，后者是脚本与文档，非 npm 包）。根目录 `package.json` 不发版，`private: true`。

### 2.2 `tsconfig.base.json` 内容

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022", "DOM", "DOM.Iterable", "WebWorker"],
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "moduleDetection": "force",
    "resolveJsonModule": true,
    "allowImportingTsExtensions": false,
    "noEmit": true,
    "isolatedModules": true,
    "verbatimModuleSyntax": true,
    "skipLibCheck": true,
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "noImplicitOverride": true,
    "forceConsistentCasingInFileNames": true,
    "esModuleInterop": true,
    "useDefineForClassFields": true
  }
}
```

各包 `tsconfig.json` 继承基础配置并补充：

| 包 | 继承后追加 |
|---|---|
| `apps/web` | `"jsx": "react-jsx"`、`"types": ["vite/client"]`、`"paths": {"@/*": ["./src/*"]}`；Web Worker 场景另立 `tsconfig.worker.json` 加 `"lib": ["ES2022","WebWorker"]` |
| `apps/server` | `"types": ["node"]`、`"composite": true`（供 tsup 构建引用） |
| `packages/shared` | `"composite": true`、`"declaration": true`、`"declarationMap": true` |

> 说明：`moduleResolution: Bundler` 与 Vite 5+ 兼容；服务端用 tsup 打包，运行时不走 tsc，故 `noEmit` 也设 true（仅做类型检查）。

---

## 3. 测试与验收总纲（各里程碑共同遵循）

### 3.1 测试分层

| 层级 | 工具 | 执行时机 |
|---|---|---|
| L1 单元 | vitest | 每次提交（pre-commit + CI） |
| L2 组件 | vitest + @testing-library/react | 每次提交 |
| L3 API/WS 集成 | vitest + fastify.inject | 每次提交 |
| L4 E2E | Playwright（Chromium） | 里程碑验收前 |
| L5 Golden 仿真一致性 | 自研运行器（M4 起建） | 里程碑验收前 |

#### 3.1.1 CI 平台与流水线

**平台**：GitHub Actions（开源仓库免费额度足够；不引入 Jenkins/CircleCI）。仓库托管在 GitHub，PR 与 push 触发 CI。

**两条流水线**：

| 流水线 | 触发 | Job | 失败处理 |
|---|---|---|---|
| `ci.yml`（PR/push） | `pull_request`、`push: branches:[main]` | ① `lint`：`pnpm lint`（eslint + prettier --check）<br>② `typecheck`：`pnpm typecheck`（全包 tsc --noEmit）<br>③ `test`：`pnpm test`（vitest run，跨 apps 与 packages） | 任一失败阻塞 merge |
| `e2e.yml`（里程碑验收前手动 dispatch） | `workflow_dispatch` | ④ `e2e`：`pnpm e2e`（Playwright Chromium，安装浏览器缓存）<br>⑤ `golden`（M4 起）：`pnpm golden`（运行器加载 examples 双引擎跑一遍） | 里程碑验收前必跑 |

配置要点：
- 使用 `pnpm/action-setup@v4` 与 `actions/setup-node@v4`（`cache: pnpm`）；
- Playwright 用 `browser-actions/setup-playwright@v1` 缓存，避免每次下载；
- `packages/shared` 改动时全量跑（`paths-filter`）；其余改动只跑受影响包的 test job（`pnpm --filter ... test`）。

### 3.2 Golden 测试机制（L5）

每个示例工程附 `golden.json` 剧本（输入序列 + 预期 GPIO 事件 + 预期串口输出 + OLED 帧哈希），运行器加载工程→指定引擎执行→比对。容差：时间 ±100ms；OLED 帧逐像素容差 ≤1%。示例见 M4。

### 3.3 缺陷分级与里程碑准出

| 级别 | 定义 | 处理 |
|---|---|---|
| S1 | 崩溃/数据丢失/安全漏洞 | 阻塞里程碑 |
| S2 | 主流程不可用 | 阻塞里程碑 |
| S3 | 次要缺陷 | 遗留 ≤3 且登记 issue |
| S4 | 体验优化 | backlog |

### 3.4 通用准出条件（每个里程碑）

1. `pnpm lint` / `pnpm typecheck` 全绿；
2. 该里程碑"测试与验收"小节全部条目通过；
3. WS/REST 协议如有变更，`packages/shared` 同步更新并评审。

#### 3.4.1 ESLint / Prettier 规则集

**ESLint 配置**（根 `eslint.config.js`，flat config）：

- 基础：`@eslint/js` 的 `recommended` + `typescript-eslint` 的 `strict`；
- 框架插件：`eslint-plugin-react`（recommended）+ `eslint-plugin-react-hooks` + `eslint-plugin-react-refresh`（仅 Vite 组件用）；
- 规则覆盖：
  - `'@typescript-eslint/no-unused-vars': ['error', { argsIgnorePattern: '^_', varsIgnorePattern: '^_' }]`
  - `'@typescript-eslint/consistent-type-imports': 'error'`（强制 `import type`）
  - `'no-console': ['warn', { allow: ['warn','error'] }]`（允许 warn/error，禁止 info/log）
  - `'react-refresh/only-export-components': 'warn'`
- 忽略：`dist/`、`build/`、`data/`、`logs/`、`config/`（运行期配置不入 lint）、`*.wasm`、`apps/web/src/sim/mpy/`（WASM 产物目录）。

**Prettier 配置**（根 `.prettierrc.json`）：

```json
{
  "printWidth": 100,
  "semi": true,
  "singleQuote": true,
  "trailingComma": "all",
  "tabWidth": 2,
  "arrowParens": "always",
  "endOfLine": "lf"
}
```

**脚本**（根 `package.json`）：
- `"lint": "eslint ."`、`"lint:fix": "eslint . --fix"`
- `"format": "prettier --write ."`、`"format:check": "prettier --check ."`
- `"typecheck": "pnpm -r run typecheck"`

**pre-commit**：用 `simple-git-hooks`（轻量，无需 husky）注册 `pre-commit` 钩子跑 `lint-staged`（仅对暂存文件跑 eslint --fix + prettier --write），失败阻止提交。配置见根 `package.json` 的 `lint-staged` 字段。

### 3.5 性能验收指标（P1/P2 终验时复核）

| 指标 | 目标 |
|---|---|
| 画布 100 元件/150 连线拖拽帧率 | ≥30fps |
| 生产构建首屏（本机） | ≤3s |
| 引擎A GPIO 1kHz 事件延迟 | ≤50ms |
| 串口 115200 满速打印 | 无丢行 |
| 4 并发会话 | 稳定 30 分钟，串口零互窜 |

### 3.6 Git 工作流

**分支策略**：简化版 GitHub Flow（不引入 Git Flow 的 develop/release 分支，单机定位足够）。

> **单人期修订（2026-08-28）**：当前为单人开发，`main` 不设分支保护，允许直推；
> 直推前须本地 lint + typecheck + test 全绿（CI push 触发复核，红即修）。
> Conventional Commits 照常执行，保证 changelog 与 tag 自动生成可用。
> 转多人协作或引入大改动/实验性改动时，恢复下表的 PR 流程与分支命名。

| 分支 | 用途 | 命名 |
|---|---|---|
| `main` | 可发布主干（单人期允许直推，多人期受保护只接 PR） | `main` |
| 功能分支 | 单个里程碑子任务开发（大改动/实验性改动） | `feat/<scope>-<topic>` 如 `feat/m2-wiring-anchor` |
| 修复分支 | 缺陷修复（影响面大时使用） | `fix/<topic>` 如 `fix/pinbus-conflict-false` |
| 示例/文档 | 不影响代码 | `docs/<topic>` / `chore/<topic>` |

**PR 规则（多人期 / 大改动）**：
- 必须经 §3.1.1 `ci.yml` 三 Job 全绿才可 merge；
- 至少 1 个 Reviewer approve（自研项目可自批，但 PR 描述必须填"实现范围/测试项/验收点"三要素对照对应里程碑）；
- Squash merge，commit message 用 [Conventional Commits](https://www.conventionalcommits.org/)（`feat:`/`fix:`/`docs:`/`refactor:`/`test:`/`chore:`）；
- 关联 issue 在 PR 描述中 link。

**模板**（`.github/pull_request_template.md`）：

```
## 实现范围
- （对照《02-实施方案》里程碑章节，列出本 PR 完成的范围）

## 测试项
- （列出新增/调整的 L1-L5 测试用例）

## 验收点
- [ ] lint/typecheck 全绿
- [ ] 里程碑"测试与验收"小节对应条目通过
- [ ] 若改 packages/shared，已同步《03-核心模块详细设计》签名

## 关联 issue
closes #
```

**版本标签**：每个里程碑验收通过后打 tag `vM<n>`（如 `vM6`），便于回滚到 P1 终验版本。

### 3.7 日志规范（前后端统一）

> 配合 §1.4 可观测性，统一前后端日志格式，便于关联排查。

**后端（pino）**：JSON 行式，每行含字段：

| 字段 | 类型 | 说明 |
|---|---|---|
| `time` | ISO8601 | pino 自动 |
| `level` | 'trace'\|'debug'\|'info'\|'warn'\|'error'\|'fatal' | 数字+字符串双写 |
| `msg` | string | 事件摘要 |
| `module` | string | 固定枚举：`build`\|`qemu`\|`ws`\|`db`\|`rest`\|`app` |
| `reqId`/`sessionId`/`buildId` | string | 链路追踪上下文（按场景选填） |
| `err` | object | error 时附 `{name,message,stack,code}` |

输出：stdout（开发期）+ `logs/server.log`（info+）+ `logs/server.error.log`（error+），轮转 10MB×5 份（pino-roll）。

**前端（console + 上报）**：
- 开发期：`console.warn/error` 走 pino-browser（同一 pino 实例，传输到 stdout via Vite overlay）；
- 生产期：捕获 `window.onerror` 与未处理 `unhandledrejection`，限频 POST `/api/ingest/frontend-error`，payload = `{msg, stack, url, line, col, ua, sessionId?, buildId?}`。

**级别约定**：
- `error`：阻塞主流程、需用户感知（引擎崩溃、编译失败、WS 断线）；
- `warn`：可降级继续（多驱动冲突、工具链缺失、路径穿越被拒）；
- `info`：关键节点（会话起停、编译起止、保存工程）；
- `debug`：仅开发期（PinBus 事件流、WS 消息原文）——生产构建剔除。

### 3.8 后端中间件链注册顺序（N14）

Fastify 插件按以下顺序在 `apps/server/src/index.ts` 注册，顺序错位会导致日志缺失、校验绕过或错误泄漏：

| 顺序 | 插件 | 作用 | 关键点 |
|---|---|---|---|
| 1 | `@fastify/cors` | 跨域 | 仅允许 `apps/web` dev origin（生产同源则 `origin: false`），不开放 `*` |
| 2 | pino logger（内置） | 请求日志 | `serializers` 屏蔽敏感字段；`level` 从 config 读，`request` 日志含 `reqId` |
| 3 | `requestId` 上下文 | 链路追踪 | 与 pino `reqId` 联动，所有日志自动带 `reqId` |
| 4 | `@fastify/rate-limit`（仅 WS 入口/编译提交路由） | 限频 | 全局默认 `max: 100/30s`；`POST /api/builds` 单独 `max: 5/30s`；`/api/ingest/frontend-error` `max: 1/10s`（§1.4 限频对齐） |
| 5 | 全局 `preHandler`：zod 校验 | 路由参数校验 | 仅路由 schema 不够用时的兜底；正常情况 Fastify `schema` 字段即触发 ajv 编译期校验，错误兜底由 `setValidationError` 转 400 |
| 6 | 路由注册（REST） | 业务路由 | `/api/health`、`/api/projects`、`/api/builds`、`/api/sessions/:id` 等 |
| 7 | `@fastify/websocket` | WS 网关 | 注册顺序在 REST 后，确保 `/ws` 不被 rate-limit 全局规则误伤；WS 连接 `preValidation` 鉴权（单机无 token，仅校验 `sessionId` 存在） |
| 8 | 全局 `setErrorHandler` | 错误兜底 | 统一响应 `{ error: { code, message, reqId } }`，500 不泄漏 stack，log.error 记录原始 stack |
| 9 | `setNotFoundError` | 404 兜底 | 未命中路由返回 404 JSON，不暴露路由表 |
| 10 | `setValidationError` | 400 兜底 | zod/ajv 校验失败统一格式 `{ error: { code: 'VALIDATION', fields: [...] } }` |
| 11 | `@fastify/static`（生产形态，§8.1） | 托管前端 | 仅 NODE_ENV=production 注册，dev 由 Vite 代理 |

**注册顺序约束**：
- CORS 必须第 1，否则后续插件的预检请求被拒；
- pino 必须在所有业务插件之前，否则请求日志缺失；
- rate-limit 必须在路由注册之前，否则部分路由绕过限频；
- errorHandler 必须在路由注册之后，否则业务抛错无法被兜底；
- `@fastify/static` 必须最后注册，否则 catch-all 路由会吞掉 API 请求。

**示例（apps/server/src/index.ts 节选）**：

```ts
import Fastify from 'fastify';
import cors from '@fastify/cors';
import rateLimit from '@fastify/rate-limit';
import websocket from '@fastify/websocket';
import staticPlugin from '@fastify/static';
import { ZodError } from 'zod';
import { restRoutes } from './routes/index.js';
import { wsGateway } from './ws/gateway.js';
import { config } from './config.js';

const app = Fastify({
  logger: { level: config.logLevel, serializers: { /* ... */ } },
  genReqId: () => crypto.randomUUID(),
});

await app.register(cors, { origin: config.corsOrigins });
await app.register(rateLimit, { max: 100, timeWindow: '30s' });
await app.register(restRoutes);              // 6
await app.register(websocket);               // 7
await app.register(wsGateway, { prefix: '/ws' });
app.setErrorHandler((err, req, reply) => {
  if (err instanceof ZodError) {
    reply.code(400).send({ error: { code: 'VALIDATION', fields: err.issues } });
    return;
  }
  req.log.error({ err }, 'unhandled error');
  reply.code(500).send({ error: { code: 'INTERNAL', message: 'Internal Server Error', reqId: req.id } });
});
app.setNotFoundHandler((req, reply) => {
  reply.code(404).send({ error: { code: 'NOT_FOUND', reqId: req.id } });
});

if (config.env === 'production') {
  await app.register(staticPlugin, { root: path.resolve('apps/web/dist') });
}

await app.listen({ port: config.port, host: '0.0.0.0' });
```

> 注意：Fastify 插件系统对注册顺序敏感（encapsulation），上述 `await` 串行确保每个插件就绪后再注册下一个；若用 `app.register(...)`（无 await），Fastify 会按调用顺序排队，但 errorHandler 与 setNotFoundHandler 是实例方法（非插件），必须在所有 register 完成后调用——故推荐显式 `await` 串行注册以便排查顺序问题。

### 3.9 前端 React ErrorBoundary 约定（N16）

前端在关键子树包裹 React ErrorBoundary，避免单个面板崩溃带倒整个工作台。约定如下：

**布局层级与 Boundary 划分**：

| Boundary 位置 | 覆盖范围 | Fallback UI | Reset 策略 |
|---|---|---|---|
| `AppRoot` | 整个应用 | 全屏错误页（含"重载"按钮 + 复制错误堆栈） | `location.reload()` |
| `CanvasBoundary`（电路画布区） | react-konva 画布 + 引脚热区 | 画布占位卡片 + "重试"按钮（保留 circuitStore 状态，仅重渲染） | 重置 `<Stage key={retryKey}>` 的 key 触发重挂 |
| `EditorBoundary`（Monaco 区） | Monaco 编辑器 + 文件 Tab | 编辑器占位卡片 + "重试"按钮 | 重新加载当前文件（不丢未保存内容，从 stores.editor 读） |
| `SerialBoundary`（串口/分析仪） | xterm.js + 逻辑分析仪 | 终端占位卡片 + "重新连接"按钮 | 新建 xterm 实例 + 重连 WS |
| `InspectorBoundary` | 属性面板 | 面板占位 + "重试" | 重渲染当前选中元件 |
| `LibraryBoundary` | 元件库面板 | 面板占位 + "重试" | 重渲染 catalog |

**错误上报**：所有 Boundary `componentDidCatch` 调用 §1.4 的 `POST /api/ingest/frontend-error`（限频 1 条/10s），payload 加 `boundary` 字段标识出错的 Boundary；`AppRoot` 的上报额外带 `recoverable: false`。

**实现示例（apps/web/src/components/ErrorBoundary.tsx）**：

```tsx
import { Component, type ErrorInfo, type ReactNode } from 'react';
import { reportFrontendError } from '../api/error';

interface Props {
  boundary: string;
  fallback: (error: Error, reset: () => void) => ReactNode;
  children: ReactNode;
}
interface State {
  error: Error | null;
}

export class ErrorBoundary extends Component<Props, State> {
  state: State = { error: null };

  static getDerivedStateFromError(error: Error): State {
    return { error };
  }

  componentDidCatch(error: Error, info: ErrorInfo): void {
    void reportFrontendError({
      msg: error.message,
      stack: error.stack,
      boundary: this.props.boundary,
      componentStack: info.componentStack,
    });
  }

  reset = (): void => this.setState({ error: null });

  render(): ReactNode {
    if (this.state.error) return this.props.fallback(this.state.error, this.reset);
    return this.props.children;
  }
}
```

**使用示例**：

```tsx
<ErrorBoundary
  boundary="canvas"
  fallback={(err, reset) => <CanvasErrorCard error={err} onRetry={reset} />}
>
  <CircuitCanvas />
</ErrorBoundary>
```

**约束**：
- 每个面板的 Boundary 必须提供明确的"重试"路径，不能只显示错误堆栈（用户无操作可恢复等于死锁）；
- `AppRoot` 的 Boundary 之外的 Boundary reset 不能 reset 整个 simStore（避免丢运行中的仿真状态）；只有 `AppRoot` 的 reset 才允许 reload 页面；
- Boundary 不捕获异步错误（Promise rejection / setTimeout 抛错）——这些由 §1.4 的 `window.onerror` + `unhandledrejection` 全局监听上报；
- WS 断线不属于 Boundary 兜底范畴（属于业务降级，由 simStore 状态机处理，见 03-§7.3）。

---

## 4. 里程碑计划（双引擎并行）

> 节奏原则：**自 M4 起，每个能力里程碑内，引擎A（MicroPython-WASM）与引擎B（QEMU+Arduino）能力对齐交付**——同一电路、同一 Golden 剧本在两个引擎下都必须通过（引擎B按其实际支持度标注例外）。
> 每个里程碑包含三要素：① 实现范围（含设计文档引用）② 测试项 ③ 验收标准与演示步骤。

### ── P1 阶段：基础闭环 ──

#### M0 准备阶段（M1 起手前一次性完成）

**① 实现范围（环境与脚手架）**
- 执行 `tools/setup-toolchain.ps1`：检测 Node 22.13+/pnpm 11/Git 2；缺失项给安装指引并退出（不自动安装，避免污染环境）；
- 初始化 git 仓库（`git init`、首提交仅含 `documents/` 与本仓库结构骨架），`.gitignore` 模板（`node_modules/`、`data/`、`logs/`、`dist/`、`build/`、`*.wasm`、`.env`）；
- 创建仓库根目录骨架：`apps/web`、`apps/server`、`packages/shared`、`config/`、`examples/`、`tools/`、`documents/` 空目录 + `.gitkeep`；
- `package.json`（根，`private:true`，`packageManager: pnpm@11.x`，`engines: {node: ">=22.13"}`）+ `pnpm-workspace.yaml`（§2.1）+ `tsconfig.base.json`（§2.2）；
- ESLint flat config + Prettier + simple-git-hooks/lint-staged（§3.4.1）；
- GitHub Actions `ci.yml` 骨架（§3.1.1，先放空 Job 占位）；
- README.md（A1）、AGENTS.md（A3）落盘。

**② 测试项**：`pnpm install` 无报错；`pnpm lint`/`pnpm typecheck` 对空包全绿；CI 在空提交上能跑通。

**③ 验收标准**
- [ ] `pnpm install` 完成且无 peer warning；
- [ ] `pnpm lint` 与 `pnpm typecheck` 全绿（空包零警告）；
- [ ] `git log` 一条首提交，`.gitignore` 排除全部产物目录；
- [ ] CI `ci.yml` 在 push 后能跑通三条 Job（即便没测试也返回绿）。

#### M1 脚手架与协议基础
**① 实现范围**
- Monorepo 初始化（pnpm workspace、tsconfig、lint）、双主题 token 框架（见《04-UI详细设计》§7）、空工作台布局（面板骨架可折叠）；
- `packages/shared`：circuit / ws-protocol / engine / catalog 四个类型文件（签名见《03-核心模块详细设计》§2）；
- 后端骨架：Fastify + `/api/health` + `/api/health/tools` + config 加载 + SQLite 连接与迁移框架；
- CI：lint/typecheck/test 三件套。

**② 测试项**
- L1：config 加载缺省值/非法值用例；迁移脚本空库升级用例；
- L3：`/api/health` 200 与 tools 自检结构。

**③ 验收标准**
- [ ] `pnpm dev` 一键启动 web(5173)+server(3001)，代理 /api、/ws 正常；
- [ ] 工作台四区布局呈现，亮/暗主题切换即时生效；
- [ ] `GET /api/health/tools` 返回真实工具链状态；
- [ ] CI 全绿。

#### M2 画布与连线（P1 元件集渲染）
**① 实现范围**
- circuitStore（parts/connections 单一数据源）；
- 画布四层渲染（Grid/Wire/Part/Overlay，见《03-核心模块详细设计》§5）；
- 元件拖放/选中/移动/旋转/删除、20px 网格吸附、缩放平移（0.25x–4x）；
- 连线：引脚拖出 → 正交布线（Wokwi `v/h/*` 路径语义）→ 吸附落点；8 色循环、编辑锚点、删除；
- 属性面板（attrs 动态表单：enum→下拉 / number→步进 / color→色板 / boolean→开关）；
- diagram.json 双向同步（画布⇄JSON 文本视图）。

**② 测试项**
- L1：wiring 算法（路径生成、锚点编辑）参数化用例 ≥20 组；circuitStore 全 action；diagram 序列化/反序列化往返一致性；
- L2：属性面板各控件类型渲染与取值；
- L4：《04-UI详细设计》§6 画布交互状态机逐状态人工核验。

**③ 验收标准**
- [ ] 使用《05-元件清单》P1 表中任 3 种元件完成"板卡+LED+按键"搭建；
- [ ] JSON 视图编辑后画布正确刷新；非法 JSON 报错且不破坏画布；
- [ ] 100 元件/150 连线压测帧率 ≥30fps（临时脚本生成）；
- [ ] 导入一个 Wokwi 简单 blink 工程可渲染（未收录元件列入跳过清单）。

#### M3 工程管理与元件配置落地
**① 实现范围**
- projects/files CRUD、启动种子（示例工程写入）、导入导出 JSON 包（formatVersion:1）；
- config/parts 与 config/boards 按《05-元件清单》落地 P1 全部元件 JSON 与 SVG 资源；
- catalog.service 启动导入 parts_catalog 表；
- 工程列表页（新建/打开/复制/删除/导入/导出）。

**② 测试项**
- L1：db 层 CRUD 与唯一约束（project_id+path）；
- L3：REST 全端点正/反向用例（含 404/400/路径穿越拒绝）；
- L2：工程列表交互。

**③ 验收标准**
- [ ] 保存→重启进程→数据完整（WAL 模式核对）；
- [ ] 导出→清库→导入完整还原；
- [ ] P1 元件全部出现在元件库面板并分类正确；
- [ ] 路径穿越用例（`../../`）返回 400。

#### M4 双引擎·串口级闭环（双引擎并行的第一个对齐点）
**① 实现范围**
- **引擎A**：MicroPython-WASM 运行时集成（构建产物入库 `apps/web/src/sim/mpy/`）；`machine.Pin`（仅输出）+ `machine.UART(0)` shim → PinBus；WASM 内存上限 256MB 与停止中断机制；
- **引擎B**：arduino-cli 编译链（build.service：队列/落库/日志流）+ esptool merge_bin 产 flash.img + QemuManager（esp-develop fork，`-serial tcp` 直连）+ WS 会话网关（attach/ctrl/uart 事件）；
- 串口监视器（xterm.js：波特率、ANSI、输入发送、环形缓冲 2MB）；
- Golden 运行器 v1（串口 + GPIO 输出断言）。

**② 测试项**
- L1：build.service 状态机（queued→running→success/failed/timeout）；QemuManager 生命周期（正常退出/强杀/端口回收）；
- L3：WS 协议全部消息正/反向（zod 自动参数化）；并发 3 编译排队；
- L5：blink Golden 在两引擎各跑一次。

**③ 验收标准（演示）**
- [ ] 同一 blink 工程：引擎A 运行 → 串口每秒 `LED ON/OFF`；切换引擎B → 一键编译 → QEMU 运行 → 相同串口输出；
- [ ] 引擎B编译失败（故意 typo）时日志可见、状态 failed；
- [ ] 会话关闭后 QEMU 进程退出、端口释放（任务管理器/进程表核对）；
- [ ] 工具链缺失时：引擎B入口置灰 + 引导提示，引擎A 不受影响。

#### M5 双引擎·GPIO 输入输出闭环
**① 实现范围**
- PinBus 完整实现（网络聚合、电平传播、多驱动冲突检测，见《03-核心模块详细设计》§4）；
- 引擎A：`machine.Pin` 输入（上拉/下拉语义）、`Pin.irq` 回调；
- 引擎B：GPIO 桥——优先 lcgamboa fork 的 GPIO 导出；若不可用则定制最小虚拟外设设备模型（GPIO 翻转 → WS `gpio.write` / 前端 `input.pin` → 注入）；
- P1 输出元件行为：LED（亮度 PWM 预留）、RGB LED、蜂鸣器（电平开关）、按键/开关（输入注入）、电阻（直通网络）、电位器（模拟值注入预留）。

**② 测试项**
- L1：PinBus 单元用例（两元件共驱同一网络冲突、开路网络读默认电平、上拉语义）；
- L5：button-led Golden（输入注入序列 → GPIO 输出序列）两引擎各跑。

**③ 验收标准（演示）**
- [ ] blink：LED 随固件 1s 闪烁，引脚电平点颜色实时变化（两引擎）；
- [ ] 按键控制 LED：按下点亮/松开熄灭（两引擎）；
- [ ] RGB LED 三色混色演示（引擎A）；
- [ ] Golden 套件：blink + button-led × 2 引擎 = 4 项全过。

#### M6 P1 终验（集成与打磨）
**① 实现范围**
- 错误面板统一（编译错误定位、运行时 traceback 展示）；
- 边界项落地（《06-边界说明》§3/§4/§7 对应条目）；
- 性能复核（§3.5 表）、P1 验收报告（按 §6 模板归档 documents/）。

**② 测试项**：P1 全部 Golden 回归 + L4 E2E 三场景 + 性能脚本。

**③ 验收标准**
- [ ] S1/S2 缺陷清零，S3 遗留 ≤3；
- [ ] P1 验收报告归档，用户签字确认。

### ── P2 阶段：完整套件（节奏同 P1：每里程碑双引擎对齐）──

#### M7 PWM / ADC（双引擎）
- 范围：引擎A `machine.PWM/ADC` shim；引擎B LEDC/ADC 桥；伺服电机元件（PWM 语义）；电位器→ADC 链路；呼吸灯/舵机示例。
- 测试：Golden pwm-breath（duty 序列断言）×2 引擎；adc 读数一致性（注入 0/2048/4095 三点）。
- 验收：[ ] 呼吸灯亮度渐变（两引擎）[ ] 电位器拖动→读数实时变化 [ ] 舵机角度正确。

#### M8 I2C / SPI 总线与传感器（双引擎）
- 范围：虚拟 I2C/SPI 总线模型（地址仲裁、寄存器语义）；DHT22（单总线）、BH1750、MPU6050（I2C）；引擎A `machine.I2C/SPI/SoftI2C` shim；引擎B虚拟 I2C 设备（SSD1306 与传感器地址覆盖）。
- 测试：总线事务回放用例；Golden sensor-read（读数断言）×2 引擎；地址冲突报错用例。
- 验收：[ ] DHT22 注入温湿度→固件读回一致 [ ] BH1750/MPU6050 读数正确 [ ] I2C 地址冲突有明确错误提示。

#### M9 显示与灯效：SSD1306 / NeoPixel（双引擎）
- 范围：framebuffer 事件协议（增量更新）；SSD1306 128×64 渲染；NeoPixel（引擎A RMT shim / 引擎B RMT 桥）；Golden 帧哈希比对启用。
- 测试：Golden oled-hello、neopixel-rainbow ×2 引擎；帧率与内存用例。
- 验收：[ ] OLED 显示文本/图形与预期帧一致 [ ] NeoPixel 彩虹流效果 [ ] 10 分钟运行内存增长 ≤200MB。

#### M10 WiFi 语义模拟（双引擎）
- 范围：network shim（引擎A：`network.WLAN` 状态机）/ 引擎B `esp_wifi` 语义模拟；内置虚拟 TCP/MQTT 服务（仅 127.0.0.1）；MQTT 发布/订阅演示工程。
- 测试：连接状态机全转移用例；MQTT pub/sub 端到端用例；安全用例（虚拟服务不监听外网）。
- 验收：[ ] 两引擎 WiFi 连接成功横幅 [ ] 固件 MQTT 发布消息在服务面板可见 [ ] 断网重连语义正确。

#### M11 逻辑分析仪（两引擎共用）
- 范围：PinBus 事件捕获（环形缓冲 10 万事件、采样上限、时间戳）、波形绘制、通道配置、游标读数。
- 测试：捕获正确性（与 Golden 事件流比对）；内存曲线用例；高频降采样告警用例。
- 验收：[ ] 1kHz PWM 波形无断档、占空比读数正确 [ ] I2C 事务波形可查看 [ ] 缓冲满时标注不崩溃。

#### M12 P2 终验
- 范围：全 Golden 回归（预计 12+ 用例 × 双引擎）、性能复核、边界抽查（《06-边界说明》§3/§4 全表）、P2 验收报告。
- 验收：[ ] 全部通过 + 报告归档。

### ── P3 阶段（按需启动，节奏另议）──

| 里程碑 | 范围 |
|---|---|
| M13 ESP-IDF 工具链 | 容器化编译，产物接入既有引擎B链路 |
| M14 多板协作 | 本地 WS 总线互联 2 板（UART/I2C 跨板） |
| M15 Wokwi 导入完善 | 元件映射表扩充、示例库扩充 |

---

## 5. 依赖清单

### 5.1 前端 `apps/web`

| 依赖 | 用途 |
|---|---|
| react / react-dom | UI |
| react-konva / konva | 画布 |
| zustand | 状态 |
| @monaco-editor/react + monaco-editor | 编辑器 |
| @xterm/xterm + addon-fit | 串口终端 |
| tailwindcss | 样式与主题 |
| nanoid | id |
| vitest + @testing-library/react / playwright | 测试 |

### 5.2 后端 `apps/server`

| 依赖 | 用途 |
|---|---|
| fastify + @fastify/websocket + @fastify/cors + @fastify/static | HTTP/WS/静态 |
| better-sqlite3 | SQLite |
| zod | 校验 |
| execa | 子进程 |
| vitest | 测试 |

### 5.3 MicroPython-WASM 运行时（M4 关键件）
- 基线：官方 `micropython/webassembly` port 产物；
- 定制：`mpconfigport` 注册自定义 `machine` C 模块（Emscripten 构建，WSL/Docker 完成，wasm 产物入库，开发机无需装工具链）；
- 降级预案：若定制构建受阻 ≥5 个工作日，M4 先以官方产物 + JS 侧 `machine` 纯 shim（通过 wasm FFI 挂钩 stdout/GC）实现 Pin/UART 最小集，后续再切换 C 模块方案。

#### 5.3.1 MicroPython-WASM 定制构建（Dockerfile）

> 在 `tools/mpy-build/` 下维护一份 Dockerfile，用 WSL/Docker 在 Linux 容器内完成 Emscripten 构建，产物 `micropython.wasm` + `micropython.wasm.mjs`（运行时 glue）入库到 `apps/web/src/sim/mpy/`，开发机无需装 Emscripten。

`tools/mpy-build/Dockerfile`：

```dockerfile
FROM emscripten/emsdk:3.1.45

# MicroPython 源码（固定 v1.26.x tag，便于复现）
RUN git clone --depth 1 --branch v1.26.0 https://github.com/micropython/micropython.git /mpy

# 定制 machine 模块：把 apps/web/src/sim/mpy/machine-shim/ 覆盖到 ports/webassembly/modules
COPY machine-shim/ /mpy/ports/webassembly/modules/machine/

WORKDIR /mpy/ports/webassembly

# 编译（产物：build-*/micropython.{wasm,wasm.mjs}）
RUN make clean && make

# 输出
CMD ["sh","-c","mkdir -p /out && cp build-*/micropython.wasm /out/ && cp build-*/micropython.wasm.mjs /out/"]
```

`tools/mpy-build/README.md` 补充使用：

```bash
# 1. 在仓库根执行
docker build -t mpy-wasm-build tools/mpy-build/

# 2. 把产物拷到 apps/web/src/sim/mpy/
docker run --rm -v "$PWD/apps/web/src/sim/mpy:/out" mpy-wasm-build

# 3. 提交 wasm 产物入库（git lfs track *.wasm）
```

`machine-shim/` 目录维护一份 C 模块占位文件（`machine.Pin`/`UART` 注册到 mp_module），具体语义由《03-核心模块详细设计》§3.2 转发表约定。M4 起按 P1 元件表逐个交付 `Pin/UART`，后续 `PWM/ADC/I2C/SPI/RMT`。

---

## 6. 验收报告模板（M6/M12 归档用）

```
验收期次 / 日期 / 环境（OS、Node、浏览器、工具链版本）
里程碑清单逐项：✅/❌/条件跳过（说明）
Golden 结果矩阵：用例 × 引擎
性能指标实测值（对照 §3.5）
遗留缺陷（S3 及以下，issue 链接）
结论：通过 / 有条件通过（整改项）/ 不通过
```

---

## 7. 质量门禁（进入下一里程碑前置）

1. §3.4 通用准出全部满足；
2. 上一里程碑验收报告已归档；
3. shared 协议变更已同步《03-核心模块详细设计》对应接口签名。

---

## 8. 发布与部署

> 单机定位，不引入 K8s/容器编排；提供两种发布形态，按用户环境选择。

### 8.1 形态一：单机源码 + Node 直接运行（推荐 P1 验收期）

适用：开发者自验、本地部署。前置：本机已装 Node 22.13+、pnpm 11+、Git、（可选）arduino-cli/QEMU。

```bash
git clone <repo> esp32-simulator && cd esp32-simulator
pnpm install
pnpm build              # 构建前端到 apps/web/dist
pnpm --filter server start   # 启动后端，监听 3001；静态托管 apps/web/dist
# 浏览器访问 http://localhost:3001
```

`apps/server` 用 `@fastify/static` 托管 `apps/web/dist`，单端口同时提供 REST/WS/前端页面，避免开发期跨域。

### 8.2 形态二：Docker Compose 一键部署（推荐 P2 终验后对外发布）

适用：交付给非开发用户、CI 自验。前置：本机装 Docker Desktop。

`docker-compose.yml`：

```yaml
services:
  app:
    build: .
    image: esp32-simulator:${VERSION:-latest}
    ports: ["3001:3001"]
    volumes:
      - ./data:/app/data          # SQLite + builds/ + flash/ 持久化
      - ./config:/app/config:ro  # 配置只读
    environment:
      - NODE_ENV=production
    restart: unless-stopped
```

`Dockerfile`（仓库根）：

```dockerfile
FROM node:20-bookworm-slim AS build
WORKDIR /app
RUN corepack enable
COPY pnpm-lock.yaml package.json pnpm-workspace.yaml ./
COPY apps packages config examples tools ./
RUN pnpm install --frozen-lockfile
RUN pnpm build

FROM node:20-bookworm-slim
WORKDIR /app
COPY --from=build /app /app
ENV NODE_ENV=production
EXPOSE 3001
CMD ["node","apps/server/dist/index.js"]
```

> 说明：`arduino-cli` 与 QEMU 工具链镜像内不预装（体积大且与宿主机强相关）；容器启动时 `tools/setup-toolchain.ps1` 等价脚本检测并提示用户挂载工具链目录或安装。`data/` 与 `config/` 走 volume 挂载，镜像本身无状态。

### 8.3 形态三：Windows 服务化（可选，P3 评估）

用 `node-windows` 或 `nssm` 把 `node apps/server/dist/index.js` 注册为 Windows 服务，开机自启；适合"教学机房"场景。P3 评估是否纳入。

### 8.4 版本与发布节奏

- 每个里程碑验收通过打 git tag `vM<n>`（§3.6）；
- P1 终验后发布 `v0.1.0`（仅引擎A + 基础外设）；
- P2 终验后发布 `v0.2.0`（完整套件 + 引擎B 双引擎对齐）；
- Docker 镜像构建在 GitHub Actions `release.yml` 中触发（`workflow_dispatch` + tag push），推送 ghcr.io；
- 发布说明从 git tag 自动生成（Conventional Commits 驱动）。
